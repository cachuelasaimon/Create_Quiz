// Import Action Types

const initialState = {
    template: '',
    quizName: '',
    subject: '',
    trial: '',
    timeLimit: '',
    itemType: {}
}

export default function (state = initialState, action) {
    switch(action.type) {
        default: return state;
    }
}